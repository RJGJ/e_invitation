import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.einvitation.app',
  appName: 'E Invitation',
  webDir: '.output/public',
}

export default config
